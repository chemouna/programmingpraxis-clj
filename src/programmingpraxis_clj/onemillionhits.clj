(ns programmingpraxis-clj.onemillionhits)

(defn print-quote-ascii-comb
  "Prints sum of the product of ascii values of adjacents chars with the difference between ascii values of head and tail."
  [x]
  )

;; this needs to return an ascii
(defn char-to-ascii
  [c]
  1)
